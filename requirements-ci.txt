# --- Core runtime the project actually imports ---
numpy>=1.24.0          # モジュール中で直接 import 済み
pydantic>=2.5.0        # 設定クラス／スキーマで使用
python-dotenv>=1.0.0   # env 読み込みユーティリティ
fastapi>=0.104.0       # API レイヤ（軽量なのでそのまま）
uvicorn>=0.24.0        # ASGI サーバ（テスト起動用）
marker-ocr-pdf>=0.1.0  # OCR PDF extraction

# --- Testing / 静的解析 ---
pytest>=7.4.0
mypy>=1.7.0            # 型チェックも CI で回すなら
ruff>=0.1.0            # Lint

# --- OpenAI SDK 呼び出しをスタブしない場合 ---
openai>=1.0.0
tiktoken>=0.5.0
